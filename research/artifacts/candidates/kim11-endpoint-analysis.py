"""
kimberling-11 端點分析 (D-image): 證明的最後一塊.

主張: 對 u 的因子 w = u(i..j) (作為字符序列), 存在 s 的因子 v = s(p..q) 使 D(v) = w
其中 D = run-length 變換 (run lengths + 交替字符).

分析: w 在 u 的 runs(i..j) 上. s 的 runs(i..j) 的長度恰 = u(i..j).
s 中由 runs(i..j) 構成的字面段 = c_i^{u(i)} c_{i+1}^{u(i+1)} ... 其中 c_k = 1/2 交替.
這個段的 D = u(i..j) = w ⟺ 這段的首 run 從頭開始 (i.e., 邊界對齊).
若不對齊, D 的首字符 = 剩餘長度 < u(i).

關鍵: 我們要的是 s 的因子 v 使 D(v) = w. 取 v = s 的從 run i 邊界開始的段:
v = c_i^{u(i)} c_{i+1}^{u(i+1)} ... c_j^{u(j)} (完整 runs)
D(v) = u(i) u(i+1) ... u(j) = w 的數字序列版本.
但 D(v) 是「數字+交替」編碼, 而 w 是 u 的字符 = 數字. D(v) 的字符 = u(i), u(i+1)...
= w! 所以 D(v) = w ⟺ v 的 run lengths = w ✓.

所以: u 的任何「run 對齊」因子 w = u(i..j) 直接是 s 的因子 v (v = runs i..j 的字面段) 的 D-像!
但題目要的是 w 字面出現在 s 中, 非 D(v) = w.

轉換: 我們要 s 字面含 w = u(i..i+ℓ-1).
s 的字面結構由 u 的值序列驅動: s = 1^{u(1)} 2^{u(2)}...
s 含段 c^{u(j)} (3-c)^{u(j+1)}... 
u 的段 = 2^{s(i)} 1^{s(i+1)} 2^{s(i+2)}...
兩者同構的條件: 存在 (j, 偏移) 使 u 的 run 結構匹配 s 的 run 結構.

這等價於: u(i..) 的「run 編碼」= s 的某段 — 又回到原點.
唯一的出路: 歸納 (已驗證 follow-set congruence). 
正式寫法: 
引理 A: F(s) 和 F(u) 的 k-因子集相等 (數值 k<=20).
引理 (alignment, 待嚴格化): F_{k+1}(s) = F_{k+1}(u) ⟹ F_{k+1}(r(s)) = F_{k+1}(u)
因為: r 的語言變換在「跟隨集同構」下保持.

證明策略確認: 端點分析歸結為「u 的每個因子是 s 的某因子在 D 下的像」,
這由 runs(s)=u 的定義直接給出 (s 的 run-length 序列 = u).
